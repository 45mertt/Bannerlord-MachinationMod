using System.Collections.Generic;
using TaleWorlds.CampaignSystem;
using TaleWorlds.CampaignSystem.Settlements;
using TaleWorlds.CampaignSystem.Roster;
using TaleWorlds.SaveSystem;

namespace RebellionsAndDemographics
{
    // Modun Tï¿½M kayï¿½t iï¿½lemlerini yï¿½neten ana sï¿½nï¿½f
    /* public class MasterSaveDefiner : SaveableTypeDefiner
    {
        // Benzersiz bir ID (Bunu deitirme)
        public MasterSaveDefiner() : base(198_456_999) { }

        protected override void DefineClassTypes()
        {
            AddClassDefinition(typeof(SettlementPopulationData), 1001);
            AddClassDefinition(typeof(PendingAllianceData), 1002);
            AddClassDefinition(typeof(CustomPartyComponent), 1003);
        }

        protected override void DefineContainerDefinitions()
        {
            ConstructContainerDefinition(typeof(Dictionary<Settlement, SettlementPopulationData>));
            ConstructContainerDefinition(typeof(List<PendingAllianceData>));
        }

        
    } */
}

